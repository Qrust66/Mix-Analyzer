__pycache__/
_tmp/

# Ableton Live
*.asd
*.als.v24.bak
Backup/
ableton/projects/*/Samples/

# Local smoke tests (not part of the test suite — run manually)
smoke_test_*.py

# Feature 3.6 CDE diagnostics dump — regenerated on every run, bulky
# (can reach several MB on busy mixes). Not worth tracking in git.
*_diagnostics.json

# graphify knowledge graph — local artifact, regenerated on demand.
# graph.html / graph.json / GRAPH_REPORT.md / cache/ all go here.
graphify-out/
.graphify_*

# Obsidian vault — local note-taking, not source of truth.
Obsidian/

# Claude Code: ignore only the per-machine permissions file.
# .claude/agents/, .claude/commands/, .claude/settings.json (shared) stay tracked.
.claude/settings.local.json
