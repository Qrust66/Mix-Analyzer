"""banque_bridge — read-only access to ableton/banque_midi_qrust.xlsx.

The user-curated MIDI/théorie banque is the **concrete material library**
that mix and composition agents pioche dans plutôt que d'inventer leurs
propres notes / patterns / vélocités. Mirror of the song_loader and
catalog_loader patterns: the .xlsx stays untouched, the loader exposes
sliced read-only access.
"""
from __future__ import annotations
